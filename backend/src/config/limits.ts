export const DAILY_LIMITS = {
  adsMinutes: 30,        // max from ads per day
  callMinutes: 120,      // max from call sessions per day
  surveyMinutes: 60,     // offerwalls
  totalMinutes: 180,     // absolute daily cap
  maxConvertMinutes: 240 // per day conversion cap
};


